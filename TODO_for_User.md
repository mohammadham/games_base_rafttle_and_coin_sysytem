# لیست وظایف کاربر پس از دریافت کد

این فایل شامل لیستی از وظایف و پیکربندی‌هایی است که شما باید پس از دریافت کدها و قبل از عملیاتی شدن کامل سیستم انجام دهید.

## 1. آماده‌سازی محیط و پایگاه داده

*   **اجرای مایگریشن‌ها:**
    دستور `php artisan migrate` را در خط فرمان پروژه خود اجرا کنید تا جداول جدید زیر در پایگاه داده شما ایجاد شوند:
    *   `api_keys`
    *   `api_transaction_logs`
    *   `coin_types`
    *   `user_coin_balances`
    *   `coin_transactions`
    *   *نکته:* اگر از پیشوند جدول در فایل `.env` خود استفاده می‌کنید (`DB_TABLE_PREFIX`)، مایگریشن‌ها باید به درستی آن را مدیریت کنند. در غیر این صورت، اگر در فایل‌های مایگریشن ایجاد شده توسط من، نام جداول نیاز به تطبیق با پیشوند خاصی دارد، لطفاً قبل از اجرای مایگریشن، آن‌ها را اصلاح کنید.

*   **(اختیاری اما پیشنهادی) اجرای Seeders:**
    *   اگر Seeders برای داده‌های اولیه (مانند کوین پایه) ایجاد شده‌اند (در این مرحله هنوز ایجاد نشده‌اند، اما در آینده ممکن است اضافه شوند)، آن‌ها را اجرا کنید: `php artisan db:seed` یا `php artisan db:seed --class=YourSpecificSeeder`.

## 2. پیکربندی‌های اولیه در پنل مدیریت / پایگاه داده

*   **پیکربندی درگاه پرداخت زرین‌پال:**
    *   به پنل مدیریت ViserGo خود بروید، بخشی مربوط به مدیریت درگاه‌های پرداخت (معمولاً زیرمجموعه "Payment Gateways" یا مشابه آن).
    *   یک درگاه جدید از نوع "Automatic Gateway" (یا معادل آن) ایجاد کنید.
    *   **Code:** مقدار دقیق `zarinpal` را وارد کنید.
    *   **Alias:** مقداری مانند `zarinpal` یا نام دلخواه دیگر.
    *   **Name:** نام نمایشی مانند "زرین‌پال" یا "ZarinPal".
    *   **Status:** فعال (Enable).
    *   **Extra Parameters (فیلد `extra` در جدول `gateways`):** این بخش بسیار مهم است. باید یک ساختار JSON معتبر با کلیدهای `merchant_id` (مرچنت کد واقعی یا تستی زرین‌پال شما) و `sandbox_mode` (مقدار `true` برای حالت تست و `false` برای حالت واقعی) وارد کنید.
        *   مثال برای حالت تست: `{"merchant_id": "YOUR_SANDBOX_MERCHANT_ID", "sandbox_mode": true, "currency_note": "پرداخت به ریال انجام خواهد شد"}`
        *   مثال برای حالت واقعی: `{"merchant_id": "YOUR_LIVE_MERCHANT_ID", "sandbox_mode": false, "currency_note": "پرداخت به ریال انجام خواهد شد"}`
    *   **Image:** لوگوی زرین‌پال را آپلود کنید.
    *   **Gateway Currencies (ارزهای درگاه):** برای این درگاه، حداقل یک ارز (مثلاً `IRT` - تومان) با تنظیمات مربوط به حداقل/حداکثر مبلغ و کارمزدها تعریف کنید. `method_code` برای این ارز باید `zarinpal` باشد.

*   **پیکربندی اولیه انواع کوین (پس از ایجاد UI مربوطه در فاز بعدی):**
    *   پس از اینکه UI مدیریت انواع کوین در پنل ادمین توسط من ایجاد شد، شما باید:
        *   حداقل یک **کوین پایه (Base Coin)** تعریف کنید. مثلاً با:
            *   `name`: "موجودی اصلی" یا "Base Currency"
            *   `code`: `MAIN` یا `BASE` (این کد در منطق برنامه برای شناسایی کوین پایه استفاده خواهد شد)
            *   `symbol`: نماد واحد پول اصلی سایت شما (مثلاً "تومان" یا "IRT")
            *   `is_base_coin`: `true` (فعال)
            *   `base_coin_value_multiplier`: باید `1.0` باشد.
            *   `status`: فعال.
        *   می‌توانید انواع کوین دیگری نیز تعریف کرده و ارزش آن‌ها را نسبت به کوین پایه مشخص کنید.

## 3. تست‌های جامع

*   **تست فارسی‌سازی و RTL:**
    *   تمامی صفحات بخش کاربری و پنل مدیریت را به دقت بررسی کنید.
    *   مشکلات مربوط به نمایش متون فارسی، چینش عناصر در حالت راست‌به‌چپ، و هرگونه مشکل ظاهری دیگر را شناسایی و گزارش دهید.

*   **تست درگاه پرداخت زرین‌پال:**
    *   با استفاده از مرچنت کد تستی زرین‌پال، یک فرآیند پرداخت کامل را از ابتدا تا انتها (شامل پرداخت موفق و ناموفق/لغو) تست کنید.
    *   بررسی کنید که آیا رکورد `Deposit` به درستی ایجاد و وضعیت آن به‌روزرسانی می‌شود.
    *   بررسی کنید که آیا موجودی کاربر (اگر پرداخت برای افزایش موجودی است) به درستی افزایش می‌یابد و رکورد `Transaction` مربوطه ثبت می‌شود.
    *   لاگ‌های سرور را برای هرگونه خطا بررسی کنید.

*   **تست API بازی‌سازان (پس از ایجاد UI مدیریت API Key):**
    *   پس از اینکه UI مدیریت API Key در پنل ادمین ایجاد شد، یک API Key تستی ایجاد کنید.
    *   با استفاده از ابزاری مانند Postman، درخواست‌هایی به endpoint های API (ابتدا `/api/v1/game/test-connection` و سپس `/api/v1/game/credit-coin` پس از تکمیل منطق آن) ارسال کنید.
    *   نحوه تولید امضا (Signature) مطابق با مستنداتی که ارائه خواهد شد، باید در سمت کلاینت تستی شما پیاده‌سازی شود.
    *   صحت عملکرد Middleware احراز هویت، پاسخ‌های API، و ثبت لاگ در `api_transaction_logs` را بررسی کنید.

*   **تست سیستم کوین (پس از ایجاد UI های مربوطه و تکمیل منطق):**
    *   تست CRUD انواع کوین در پنل ادمین.
    *   تست مشاهده و اصلاح موجودی کوین کاربران توسط ادمین.
    *   تست تاریخچه تراکنش‌های کوین.
    *   تست عملیات `creditCoin` و `debitCoin` از طریق API (پس از تکمیل).
    *   تست استفاده از کوین در بخش‌های قرعه‌کشی و خرید آیتم (پس از پیاده‌سازی).

## 4. تکمیل و توسعه UI

*   بخش‌های قابل توجهی از رابط کاربری (UI) برای قابلیت‌های جدید (مانند فرم‌های پرداخت، نمایش موجودی کوین‌ها، مدیریت API Key و انواع کوین در پنل ادمین) توسط من به صورت اولیه ایجاد خواهد شد. اما ممکن است نیاز به بهبود، استایل‌دهی نهایی مطابق با قالب ViserGo شما، و افزودن جزئیات بیشتر توسط شما یا تیم UI شما باشد.

## 5. موارد امنیتی

*   اطمینان حاصل کنید که `secret_key` های مربوط به API Key ها به صورت امن مدیریت می‌شوند.
*   به طور منظم لاگ‌ها (سرور، API، تراکنش‌ها) را برای فعالیت‌های مشکوک بررسی کنید.
*   سایر توصیه‌های امنیتی استاندارد برای برنامه‌های تحت وب را رعایت کنید.

این لیست به مرور و با پیشرفت پروژه تکمیل‌تر خواهد شد. لطفاً در صورت داشتن هرگونه سوال یا نیاز به راهنمایی در مورد این وظایف، اطلاع دهید.
